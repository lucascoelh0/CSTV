pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "GO Matches"
include(":app")
include(":domain")
include(":data")
include(":core")
